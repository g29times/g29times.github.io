{
  "name": "aimmar-blog-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "seed:sql": "node ./seed.mjs > ./seed.sql"
  },
  "devDependencies": {
    "wrangler": "^3.114.0",
    "@cloudflare/workers-types": "^4.20250129.0",
    "typescript": "^5.8.3"
  }
}
