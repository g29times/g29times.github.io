# TESTING.md
- **Manual Verification**: Use curl for API health checks and D1 connectivity.
- **Workflow Testing**: Monitor cron trigger status in logs and `STATE.md`.
- **Unit/E2E**: Rely on `Vitest` and `gsd-tools.cjs verify` suite for consistency.
